name: "🔬 Clean Eating Scraper — Kupi.cz"

on:
  schedule:
    - cron: "0 5 * * 1"
    - cron: "0 5 * * 3"
    - cron: "0 5 * * 4"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - name: "📥 Checkout repozitár"
        uses: actions/checkout@v4

      - name: "🐍 Setup Python"
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: "📦 Inštalácia závislostí"
        run: pip install -r clean-eating-app/requirements.txt

      - name: "🔬 Spustenie scrapera"
        run: python clean-eating-app/scraper/kupi_scraper.py --output clean-eating-app/data

      - name: "📊 Kontrola výsledkov"
        run: |
          echo "=== Súbory v data/ ==="
          ls -la clean-eating-app/data/
          echo ""
          echo "=== Produkty ==="
          python -c "
          import json, glob
          total = 0
          for f in sorted(glob.glob('clean-eating-app/data/products*.json')):
              with open(f) as fh:
                  d = json.load(fh)
                  n = len(d.get('products', []))
                  total += n
                  print(f'  {f}: {n} produktov')
          print(f'  CELKOM: {total}')
          "

      - name: "💾 Commit a push nových dát"
        run: |
          git config user.name "Clean Eating Bot"
          git config user.email "bot@clean-eating.app"
          git add clean-eating-app/data/
          
          if git diff --staged --quiet; then
            echo "Žiadne nové dáta — skip"
          else
            DATUM=$(date +%Y-%m-%d)
            git commit -m "🔄 Aktualizácia letákov $DATUM"
            git push
          fi
